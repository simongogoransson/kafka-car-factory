FROM node:20-alpine

WORKDIR /app

COPY package.json ./
RUN npm install --omit=dev

COPY src/ ./src/

ENV NODE_ENV=production
ENV HTTP_PORT=3000
ENV WS_PORT=3001

EXPOSE 3000 3001

USER node

CMD ["node", "src/index.js"]
